<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- No ActionBar: the PWA draws its own header, and a native bar on top of
         it would be a second one. -->
    <style name="Theme.Quantix" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
